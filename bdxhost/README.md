# bdx Electron Window Host

Requires a compiled version of bdx Native under `bin/` and node >= 9

## Setup (yarn required)

``
yarn install
``

## Run locally

``
yarn run dev
``

## Build Installer

`` 
yarn run package
``